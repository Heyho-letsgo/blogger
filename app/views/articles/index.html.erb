<h1>Affiche tout les éléments de la Table Article</h1>

<ul id="articles">
	<% @articles.each do |article| %>
		<li>
			<%= link_to article.title, article_path(article)%> <br>
			<%= article.body %>
		</li>
	<%end%>
</ul>
<br>
<%= link_to "Créer un nouvel article", new_article_path %>
